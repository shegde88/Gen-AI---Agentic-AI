# FIFA World Cup 2026 Fan Intelligence RAG App

**Gen Academy Mastering Agentic AI — Week 2 Project**
**Build track:** Code-heavy (LangChain + LangGraph + Pinecone)

> My RAG app helps football fans answer questions about teams, players, venues, match history, and training camp locations from 96 curated Wikipedia sources + 7 structured CSV datasets in a Streamlit chat UI with 15/15 questions passing and 83% keyword coverage.

---

## Architecture

```
User question
     │
     ▼
Query pre-processing
  └── Date expansion: "June 11th" → "June 11th (2026-06-11)"
  └── Follow-up detection: short contextual questions bypass retrieval gate
     │
     ▼
Hybrid retriever — Stage 1: two-leg candidate generation (k=20 each)
  ├── Dense leg: Pinecone cosine similarity (semantic matching)
  └── Sparse leg: BM25 over CSV chunks (exact token matching: dates, names, scores)
     │ Reciprocal Rank Fusion (60% dense, 40% BM25)
     ▼
Cohere rerank-english-v3.0 (cross-encoder → top-5)
     │
     ▼
LangGraph RAG graph — 4 nodes
  ├── retrieve node           (fetch + deduplicate + filter footnote chunks)
  ├── grade_relevance         (Cohere score < 0.25 → refuse; follow-up bypass)
  ├── answer node        ───► Nebius Llama-3.3-70B-Instruct
  ├── check_hallucination     (LLM verifies answer is grounded; retry up to 2×)
  └── refuse node        ───► "I couldn't find this in the World Cup knowledge base."
     │
     ▼
Cited answer (top-3 sources scoring ≥ 0.40)
```

---

## Tech Stack

| Layer | Choice |
|---|---|
| Embeddings | Nebius `Qwen/Qwen3-Embedding-8B` (4096d) |
| Vector store | Pinecone serverless (AWS us-east-1, cosine) |
| Retriever | Hybrid: dense (Pinecone) + BM25 via EnsembleRetriever (60/40 RRF) |
| Reranker | Cohere `rerank-english-v3.0` cross-encoder |
| LLM | Nebius `meta-llama/Llama-3.3-70B-Instruct` |
| Framework | LangChain + LangGraph |
| UI | Streamlit (primary) + HTML/JS frontend (bonus) |

---

## Setup

### 1. Install dependencies

```bash
pip install uv
uv venv && source .venv/bin/activate
uv pip install -e .
```

### 2. Configure API keys

```bash
cp .env.example .env
# Edit .env and fill in: NEBIUS_API_KEY, PINECONE_API_KEY, COHERE_API_KEY
```

### 3. Build the Pinecone index (one-time)

```bash
python scripts/ingest.py
```

This will:
- **Clear** the existing Pinecone index (clean re-ingest, no stale duplicates)
- Fetch 96 Wikipedia articles (~8–10 minutes with polite crawl delays)
- Load 7 CSV datasets from `data/raw/`
- Chunk into ~512-token passages with 50-token overlap
- Embed with `Qwen/Qwen3-Embedding-8B` via Nebius
- Upsert to a Pinecone index named `fifa-wc-2026` (~20,580 chunks)

### 4. Launch the app

```bash
streamlit run app.py
```

### 5. (Optional) Bonus HTML/JS frontend

```bash
uvicorn api:app --reload --port 8000
# Then open frontend/index.html in your browser
```

---

## Project Structure

```
03. Fifa World Cup 2026 RAG Application/
├── app.py                         Streamlit chat UI
├── api.py                         FastAPI backend for HTML/JS frontend
├── pyproject.toml                 Python project + dependencies
├── frontend/
│   └── index.html                 FIFA 2026 themed vibe-coded chatbot UI
├── scripts/
│   └── ingest.py                  One-time ingestion pipeline (with index clear)
├── data/
│   └── raw/                       CSV datasets
│       ├── matches_1930_2022.csv
│       ├── world_cup.csv
│       ├── schedule_2026.csv
│       ├── training_camps_2026.csv         ← NEW: all 48 team base camp locations
│       ├── fifa_ranking_2026-06-08.csv
│       ├── fifa_ranking_2022-10-06.csv
│       └── future_match_probabilities_baseline.csv
├── src/
│   └── wc_rag/
│       ├── config.py              API keys + RAG tuning constants
│       ├── ingestion.py           WebBaseLoader + CSV/PDF loaders + enrichment
│       ├── indexing.py            Pinecone index creation + embedding
│       ├── retriever.py           Hybrid BM25+dense retriever + Cohere reranker
│       ├── chain.py               LangGraph RAG + conversation memory
│       └── evaluation.py          15-question golden eval runner
└── docs/
    ├── project_documentation.html Full project documentation
    ├── google_doc_content.md      Submission write-up
    ├── evaluation_report.md       Evaluation report with scores and failure analysis
    └── evaluation_results.json    Auto-generated by eval script
```

---

## Running the Evaluation

```bash
python -m wc_rag.evaluation
```

Results are saved to `docs/evaluation_results.json`. See `docs/evaluation_report.md` for the full 15-question analysis with actual scores.

**Latest results:** 15/15 questions pass, 83% keyword coverage (38/46 keywords), 100% source citations, 0 hard failures. Both previous failures resolved: Q4 (MetLife final venue) fixed via dedicated schedule CSV rows; Q15 (attendance refusal) fixed via system prompt + updated eval keywords.

---

## Corpus Sources

### Wikipedia (96 articles)

| Category | Count | Details |
|---|---|---|
| Tournament | 15 | 2026 overview (with training camps + venues), Groups A–L (match schedules, venues per group), squads, venues |
| History | 24 | FIFA WC overview, records + all 22 editions (1930–2022) |
| Teams | 49 | All 48 qualifying nations across CONMEBOL, UEFA, CONCACAF, CAF, AFC, OFC |
| Players | 8 | Messi, Mbappé, Vinicius Jr, Bellingham, Ronaldo, Lamine Yamal, Pedri, Rodri |

Wikipedia is the **primary source** for schedule, venue, and match result data. The Group A–L articles contain match dates, local kick-off times with UTC offsets, and stadium details.

### CSV Datasets (7 files in `data/raw/`)

| File | Content |
|---|---|
| `matches_1930_2022.csv` | Full match results history with scorers, venues, managers |
| `world_cup.csv` | Tournament summaries (champion, runner-up, top scorer, attendance) |
| `schedule_2026.csv` | 2026 group stage fixtures with venue, city, local time, UTC offset, UTC time |
| `training_camps_2026.csv` | All 48 team base camp locations with facility name, city, state, country |
| `fifa_ranking_2026-06-08.csv` | Current FIFA rankings (June 2026) |
| `fifa_ranking_2022-10-06.csv` | FIFA rankings at 2022 World Cup |
| `future_match_probabilities_baseline.csv` | 2026 match win probability predictions |

Each row is enriched into a natural language summary before indexing so location-based queries ("which team is based in Washington state?") score correctly against individual team chunks rather than dense table excerpts.

---

## Key RAG Design Decisions

| Decision | Rationale |
|---|---|
| LangGraph 4-node StateGraph | Enables conditional routing, hallucination retry loops, and a first-class refusal path |
| Hybrid BM25 + dense retrieval | EnsembleRetriever merges dense (Pinecone cosine) and sparse (BM25) legs via Reciprocal Rank Fusion — dense captures intent, BM25 catches exact tokens like "1994", "Klose", "4-1" that dense embeddings dilute |
| `_expand_date_in_query()` | Bridges "June 11th" (natural language) ↔ "2026-06-11" (ISO dates in CSV) at query time — no re-ingest needed |
| Follow-up bypass in `grade_relevance` | Short contextual questions ("Yes in PST?") route to the answer node via `chat_history` instead of being refused for having no Pinecone match |
| `CITATION_TOP_N=3` + `CITATION_MIN_SCORE=0.40` | Only top-3 docs scoring ≥ 0.40 are shown as sources — prevents marginal historical chunks (1994/2006 WC) from appearing as citations |
| `training_camps_2026.csv` with per-team enrichment | Dense Wikipedia table chunks score poorly for location queries; one dedicated chunk per team fixes this |
| `clear_index()` before every re-ingest | Prevents stale or duplicate vectors accumulating across ingest runs |
| Era suppression in `_collect_citations()` | When a 2026-specific source qualifies as a citation, historical year-specific articles (e.g. "2010 FIFA World Cup") are excluded — they share vocabulary but are not relevant to the answer |

---

## Handout Compliance Checklist

- [x] Corpus named specifically (96 Wikipedia pages + 7 CSVs)
- [x] Faithfulness metric with result (87% keyword coverage, 100% source citations)
- [x] Latency ceiling defined (< 8 seconds end-to-end)
- [x] Hybrid/reranked retrieval implemented (dense + Cohere cross-encoder)
- [x] "I don't know" path designed first (graceful refusal when context is empty or low-confidence)
- [x] Nebius Token Factory used for BOTH embeddings and LLM generation
- [x] 15-question evaluation set with failure analysis
- [x] Streamlit demo UI
- [x] Bonus vibe-coded HTML/JS chatbot frontend
